import type { NextPage } from 'next'

import Layout from '../components/Layout';

const Game: NextPage = () => {
    return (
        <Layout>
            Game
        </Layout>
    );
}

export default Game
